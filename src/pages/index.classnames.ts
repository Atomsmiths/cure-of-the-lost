import s from "@styles/design-system.module.css";
import classNames from "classnames";
import classnames from "classnames";

export const HOMEPAGE_TITLE_CLASSNAMES = classnames(s["homepage-title"]);
export const HOMEPAGE_MENU_ITEM_CLASSNAMES = classnames(s["menu-item"]);
export const HOMEPAGE_NAV_CLASSNAMES = classnames(s["nav"]);
export const MAP_GRID = classNames(s["grid"]);
export const MAP_GRID_ITEM = classNames(s["grid-item"]);
export const MAP_SHOP = classNames(s["shop"]);
export const MAP_EMPTY = classNames(s["empty"]);
